#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * argstostr - check the code for Holberton School students.
 *@ac: the variable
 *@av: another variable
 *Return: dest
 */
void *malloc_checked(unsigned int b)
{
	void *s;

	s = malloc(b);
	if (str == NULL)
		exit(98);
	return (s);
}
