#include "holberton.h"
/**
 * _strcmp - check the code for Holberton School students.
 *@s1: the variable
 *@s2: another variable
 *Return: dest
 */
int _strcmp(char *s1, char *s2)
{
	if (*s1 > *s2)
		return ((int) *s1 - *s2);
		else
			return ((int) *s1 - *s2);
}
